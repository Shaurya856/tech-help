#include "process_file.h"

#include <iostream>

// Placeholder entry point. The real native single-window UI
// (file picker / drag-and-drop -> process_file -> result screen)
// replaces this stub.
int main() {
    std::cout << "tech-help UI placeholder\n";
    return 0;
}
