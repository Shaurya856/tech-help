#include "process_file.h"

#include <iostream>

// Placeholder entry point. The real watcher (IMAP check -> process_file ->
// SMTP reply, run once per invocation by Task Scheduler) replaces this stub.
int main() {
    std::cout << "tech-help email-watcher placeholder\n";
    return 0;
}
